# Framework Context Engineer

Read `../../src/roles/framework-context-engineer.md` first.
Use it as the source of truth.

In this repository, resolve `framework_checkout_root/src/**` references as `./src/**`.
This wrapper exists only to bind the runtime role to framework-repo work.

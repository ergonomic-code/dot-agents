# Spring context

Read `../conventions/spring.md`.
When the work adds or changes Spring HTTP JSON API error handling or error-body contracts, also read `../conventions/spring-http-json-api.md`.
Project-specific Spring MVC handler rules remain project-local context and are loaded through project `AGENTS.md` when declared there.

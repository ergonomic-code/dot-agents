flowchart LR
    subgraph publication["Publication"]
        prepare["Prepare publication"]
        select_recipients["Select recipients"]
    end

    delivery["Deliver publication"]
    status["Track delivery status"]

    prepare --> select_recipients
    select_recipients -->|"publication and recipients"| delivery
    delivery -->|"delivery result"| status

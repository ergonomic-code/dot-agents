interface:
  display_name: "Select Next Increment"
  short_description: "Classify and select the next minimal task step"
  default_prompt: "Use $select-next-increment with a task directory to select its next minimal unfinished step and classify whether it requires TDD."

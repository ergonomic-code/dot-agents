interface:
  display_name: "Write Verification Check"
  short_description: "Write concise Feature/Rule/Example verification checks"
  default_prompt: "Use $write-verification-check to write or normalize `test-case-specification-format` Feature/Rule/Example checks in `full` mode with abstract, contract-based wording and without unnecessary concrete values."

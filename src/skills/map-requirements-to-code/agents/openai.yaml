interface:
  display_name: "Map Requirements to Code"
  short_description: "Create an auxiliary requirements-to-code map"
  default_prompt: "Use $map-requirements-to-code to create or update a requirements-to-code mapping from this feature brief by linking its requirements to concrete code anchors and future change points in a required two-column table."

interface:
  display_name: "Collect Code Anchors"
  short_description: "Create an auxiliary behavior-to-code anchor list"
  default_prompt: "Use $collect-code-anchors to create or update a Markdown code-anchor list from explicit target behavior, code starting points, and optional output path. If no code starting points are present, ask me for them before searching."

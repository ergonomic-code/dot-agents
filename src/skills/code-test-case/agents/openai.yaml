interface:
  display_name: "Code Test Case"
  short_description: "Generate or align Kotlin JUnit 5 tests from prose cases or progress items"
  default_prompt: "Use $code-test-case to generate Kotlin JUnit 5 tests from this `verification-check-format-v0.1` Feature/Rule/Example artifact in `full` mode, or from the next/следующий or selected feature progress/checklist case backed by one."

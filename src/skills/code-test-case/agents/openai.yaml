interface:
  display_name: "Code Test Case"
  short_description: "Generate or align Kotlin JUnit 5 tests and verify their behavior state"
  default_prompt: "Use $code-test-case to generate or align a Kotlin JUnit 5 test, then verify whether it is expected red or already green."

interface:
  display_name: "Fix Red Case"
  short_description: "Fix one red case through production code"
  default_prompt: "Use $fix-red-case after $code-test-case when the selected Kotlin JUnit case is red and only production code may be changed."

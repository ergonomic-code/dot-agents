# Agents

## Framework

This repository uses the Ergocode AI Agent Framework.
Framework checkout root: `<framework-checkout-root>`.
Framework config path: `<framework-config-path>`.
Keep the framework Codex `SessionStart` hook enabled.
The hook loads `<framework-checkout-root>/src/project-baseline.md`, then `<framework-checkout-root>/src/context/index.md`, then optional framework config before the local instructions in this file.

TODO: add repository-specific sections below
